const elementR = document.getElementById('user_button_rock');
const elementP = document.getElementById('user_button_paper');
const elementS = document.getElementById('user_button_scissors;');

const element_computer = ["ROCK", "PAPER", "SCISSORS"];

const computer_choice = Math.floor(Math.random() * 3);


elementR.addEventListener('click', )

function button_behaviour(){
    // something here to make it do something
}